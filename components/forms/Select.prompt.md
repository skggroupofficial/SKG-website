**Select** — labelled native select with the SKG chevron.

```jsx
<Select label="Enquiry type" placeholder="Choose one"
  options={['Land / JV', 'Investment', 'Hospitality', 'Careers']} />

<Select label="Division" options={[{value:'dev',label:'Development'},{value:'host',label:'Hospitality'}]} />
```
